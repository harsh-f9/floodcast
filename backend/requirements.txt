fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
httpx>=0.24.0
# NOTE: torch is pinned to the 2.2 CPU wheel (small RSS). Newer torch (2.14+)
# OOM-kills the worker on Render's 512MB free tier at first inference.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
xgboost>=2.1.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.0.0
joblib>=1.3.0
requests>=2.31.0
APScheduler>=3.10.0
pytz>=2023.3
# GloFAS boot anchor (baseflow_glofas.py; lazy-imported, boot skips cleanly if absent)
cdsapi>=0.7.0
xarray>=2024.1.0
cfgrib>=0.9.0
eccodes>=2.30.0

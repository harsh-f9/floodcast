# Phase 05-delivery — SPEC.md
Stamp model_version (from model_config.json) + db_date + generation_id on /predict. Unify dual APIs: keep real /predict, mark dummy /api/predict demo-only. Alerts: additive /api/admin/alert-webhook (Telegram/email on DANGER/EXTREME) via env. Replace prompt()=="cro" with rate-limit+confirm. Wire one ProjectDetail flood page live.
